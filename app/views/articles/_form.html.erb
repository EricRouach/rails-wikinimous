<%= form_for(@article, class: "form-group") do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title, class: "form-control"%> <br>
  <%= a.label :content %>
  <%= a.text_area :content, class: "form-control" %> <br>
  <%= a.submit 'Submit', class: "btn btn-primary" %>
<% end %>
